server:
  port: 8879
spring :
  application :
    name : auth-service
  cloud :
    config :
      uri : http://${CONFIGURATION_HOST:localhost}:${CONFIGURATION_PORT:8888}
      fail-fast : false
      label : ""
